services:
  - type: web
    name: flowworks-assistant
    runtime: python
    buildCommand: ""
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false
